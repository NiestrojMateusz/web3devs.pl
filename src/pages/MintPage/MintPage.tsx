const MintPage = () => {
  return (
    <>
      <button onClick={() => {
	      console.log("Connect to MetaMask clicked")
      }}>Connect to MetaMask</button>
    </>
  );
};

export default MintPage;
